const router = require("express").Router();
const db = require('../../models');

router.post("/create", (req,res) => {
  // const commentObj = {
  //   name: req.body.name,
  //   message: req.body.message
  // }

  // db.Comment.create(commentObj).then(result)


})


module.exports = router;